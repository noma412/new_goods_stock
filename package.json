{
  "name": "new_goods_stock",
  "version": "1.0.0",
  "description": "correct info of new goods about convenience store",
  "main": "dist/app.js",
  "scripts": {
    "start:local": "cross-env NODE_ENV=local nodemon dist/local.js",
    "start:prod": "cross-env NODE_ENV=prod nodemon dist/local.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "mysql": "^2.18.1",
    "puppeteer": "^12.0.1"
  },
  "devDependencies": {
    "@types/mysql": "^2.15.19",
    "@types/node": "^16.11.6",
    "cross-env": "^7.0.3",
    "nodemon": "^2.0.14"
  }
}
